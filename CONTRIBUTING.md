# Contributing

- Do not commit `backend/.env` or any file containing real API keys. Use [`backend/.env.example`](backend/.env.example) as the template and keep secrets in your local `backend/.env` only.
- See [SECURITY.md](SECURITY.md) for reporting security issues.
