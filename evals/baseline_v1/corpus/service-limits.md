# 星河发布平台：接口与上传限制

本文档是合成评测资料，不描述任何真实系统或用户。

单个上传文件最大为 200 MiB，每个批次最多包含 20 个文件。同一租户最多同时运行 4 个摄取任务。普通 API 调用限制为每分钟 60 次；超过限制时服务返回 429。

客户端收到 429 后应按 1 秒、2 秒、4 秒进行指数退避，最多重试 3 次，并遵守响应中的 `Retry-After`（若存在）。文件过大属于参数错误，不应通过重试解决。
